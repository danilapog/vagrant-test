checks:
  doNotAutoAddDefaults: true
  include:
    - "dangling-service"
    - "deprecated-service-account-field"
    - "docker-sock"
    - "duplicate-env-var"
    - "env-var-secret"
    - "exposed-services"
    - "invalid-target-ports"
    - "latest-tag"
    - "privileged-ports"
