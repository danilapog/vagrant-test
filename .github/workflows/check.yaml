name: Sboam test

on:
  push:

  
jobs:
  sync:
    name: "Sync"
    runs-on: macos-15-large
    steps:
    - name: Checkout code 
      uses: actions/checkout@v4

    - name: check freespace
      run: |
        df -h
