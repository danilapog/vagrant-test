name: Upload Release Files to AWS S3

on:
  push:
  
jobs:
  upload-release:
    name: "Upload Release Files to AWS S3"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: echo
        run: |
            echo aboba1
            echo aboba2
            #echo aboba3
